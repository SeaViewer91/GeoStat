# 줄바꿈: 저장소에는 LF로 두고, Windows에서 클론해도 셸 스크립트는 LF를 유지함 (CRLF면 bash가 실행하지 못함)
* text=auto eol=lf
*.sh text eol=lf
*.bat text eol=crlf
*.ps1 text eol=crlf

# 바이너리 (줄바꿈 변환 금지)
*.png binary
*.ico binary
*.icns binary
*.gpkg binary
*.shp binary
*.shx binary
*.dbf binary
*.tif binary
*.tiff binary
